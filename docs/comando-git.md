# Comandos GitHub
Referencia del repositorio local con el repositorio remoto (GitHub)
git remote add origin https://github.com/Damian0106/back_nest_angularDLl

# Inicializar el repositorio local
```
git init
```
- Referencia del repositorio local con el repositorio remonto (GitHub)
````
git remote add origin https://github.com/Damian0106/back_nest_angularDLl.git
````

# Validar referencia 
````
git remote -v
````
-----------------
```
git add .
git commit -m "proyecto base"
git push origin master 
```
- Para forzar

git push -f origin master 
```